function [ fact ] = GetBPFactor(idx,ii,jj,TT,AA,nNeurons)
% Need to recursively find all paths from theta to the end

nLayers = length(nNeurons);



if(idx == nLayers)
    fact = 1;
elseif(idx == 1)
    
else
    
end




end

